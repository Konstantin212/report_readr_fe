#!/bin/sh
# Pre-push gate: block the push if any check fails.
set -e
echo "▶ pre-push: typecheck"
pnpm typecheck
echo "▶ pre-push: lint"
pnpm lint
echo "▶ pre-push: test"
pnpm test
echo "▶ pre-push: build"
pnpm build
echo "✓ pre-push checks passed"
